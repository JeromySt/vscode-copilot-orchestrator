{
  "name": "aio-extension-tests",
  "version": "0.1.0",
  "private": true,
  "description": "Smoke tests for the TypeScript VS Code extension transport layer (job 040).",
  "scripts": {
    "test": "mocha --require ts-node/register --timeout 10000 \"**/*.test.ts\""
  },
  "devDependencies": {
    "@types/mocha": "^10.0.6",
    "@types/node": "^20.11.0",
    "mocha": "^10.2.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
