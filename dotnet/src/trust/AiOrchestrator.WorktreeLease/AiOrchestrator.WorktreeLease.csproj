<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <AssemblyName>AiOrchestrator.WorktreeLease</AssemblyName>
    <RootNamespace>AiOrchestrator.WorktreeLease</RootNamespace>
    <!--
      OE0011 suppression — scoped to this project only.
      This project implements CAS-under-lock lease semantics (LS-CAS-1) which requires
      System.IO.FileStream with FileShare.None. IFileSystem does not expose open-or-create
      with exclusive share, so CasLeaseStore is the sole allowed consumer here.
    -->
    <WarningsNotAsErrors>OE0011</WarningsNotAsErrors>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="AiOrchestrator.WorktreeLease.Tests" />
  </ItemGroup><ItemGroup>
    <PackageReference Include="Microsoft.Extensions.DependencyInjection.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
    <PackageReference Include="Microsoft.Extensions.Options" />
  </ItemGroup>

  <ItemGroup>
    <AdditionalFiles Include="PublicAPI.Shipped.txt" />
    <AdditionalFiles Include="PublicAPI.Unshipped.txt" />
  </ItemGroup>


  <ItemGroup>
    <ProjectReference Include="..\..\core\AiOrchestrator.Models\AiOrchestrator.Models.csproj" />
    <ProjectReference Include="..\..\core\AiOrchestrator.Abstractions\AiOrchestrator.Abstractions.csproj" />
  </ItemGroup>
</Project>
