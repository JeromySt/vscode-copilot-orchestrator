/**
 * @fileoverview Plan selector control.
 *
 * Manages plan selection checkboxes and state.
 *
 * @module ui/templates/release/scripts/planSelector
 */

export function renderPlanSelector(): string {
  return `
    // Plan selector logic is embedded in the main scriptsTemplate
    // See PlanSelectorControl class
  `;
}
