/**
 * @fileoverview Wizard navigation control.
 *
 * Handles step-by-step navigation through the release wizard.
 *
 * @module ui/templates/release/scripts/wizardNavigation
 */

export function renderWizardNavigation(): string {
  return `
    // Wizard navigation is handled by the main control logic
    // and delegated to the extension for state management
  `;
}
