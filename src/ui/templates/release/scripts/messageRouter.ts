/**
 * @fileoverview Message router.
 *
 * Routes messages between webview and extension.
 *
 * @module ui/templates/release/scripts/messageRouter
 */

export function renderMessageRouter(): string {
  return `
    // Message routing is handled in the main scriptsTemplate
    // See window.addEventListener('message', ...) handler
  `;
}
